# 销售数据分析智能体 — 文件清单

> 本清单按照「从整体到局部」的逻辑组织，适合逐层学习理解项目。
> 每个文件标注了它在系统中的角色和所属架构层级。

---

## 项目全景

```
数据处理分析智能体/
│
├── 📖 CLAUDE.md                ← AI 助手的项目说明书（先看这个）
├── 📖 文件清单.md               ← 本文件，项目地图
├── 🔒 .gitignore               ← Git 版本管理忽略规则
│
├── 📁 backend/                 ← 【后端】智能体核心系统（数据分析引擎）
│   └── 详见「第一部分」
│
└── 📁 dashboard/               ← 【前端】Streamlit 可视化管理面板
    └── 详见「第二部分」
```

## 两大系统如何协作

```
dashboard/（操作面板）          backend/（分析引擎）
    │                              │
    │  用户点击「启动执行」           │
    │         │                    │
    │         ▼                    │
    │  agent_service.py            │
    │  在后台线程调用 ──────────▶  main.py 的全流程
    │                              │
    │  用户查看「数据概览」          │
    │         │                    │
    │         ▼                    │
    │  data_service.py ─────────▶ MySQL → 统计数据
    │                              │
    └──────────────────────────────┘
         dashboard/ 通过 pymysql      backend/ 通过 pymysql
         直连 MySQL 查询展示           直连 MySQL 做分析
```

---

# 第一部分：backend/ — 智能体核心系统

## 目录结构

```
backend/
├── main.py                      ← 🚀 主入口（命令行启动）
├── config.json                  ← ⚙️ 配置文件（真实密钥，不入 git）
├── config.example.json          ← 📋 配置模板（给新环境参考）
├── requirements.txt             ← 📦 Python 依赖清单
├── setup.bat                    ← 🔧 一键安装依赖
├── setup_task.bat               ← ⏰ 一键配置 Windows 定时任务
├── generate_test_data.py        ← 🧪 测试数据生成工具
│
├── src/                         ← 🔬 核心分析引擎
│   ├── __init__.py
│   ├── data_fetcher.py          ← ① 数据拉取
│   ├── anomaly_detector.py      ← ② 异常检测
│   ├── analyzer.py              ← ③ AI 归因分析
│   ├── reporter.py              ← ④ 报告生成
│   └── notifier.py              ← ⑤ 推送通知
│
├── scripts/                     ← 🛠️ 辅助工具
│   ├── __init__.py
│   ├── init_db.sql              ← MySQL 建表脚本
│   └── export_mysql_to_excel.py ← MySQL → Excel 导出
│
├── data/                        ← 📁 Excel 数据文件
├── reports/                     ← 📄 生成的 Markdown 报告
└── logs/                        ← 📝 运行日志
```

## 文件逐一说明

### 入口与配置

| 文件 | 作用 | 通俗类比 |
|------|------|----------|
| `main.py` | 整个系统的指挥中心，按顺序调用 5 个步骤 | 工厂的生产流水线总开关 |
| `config.json` | 所有可调参数集中存放（数据源、阈值、密钥） | 驾驶舱的仪表盘 |
| `config.example.json` | 同上，但密钥用占位符替代 | 驾驶舱说明书（给新司机看） |
| `requirements.txt` | 列出所有需要的 Python 第三方库 | 购物清单 |
| `setup.bat` | 双击自动安装所有依赖 | 一键采购 |

### src/ 核心分析引擎（学习重点）

这 5 个文件构成了**数据分析流水线**，按顺序执行：

| 步骤 | 文件 | 核心函数 | 输入 | 输出 |
|------|------|----------|------|------|
| ① | `data_fetcher.py` | `fetch_sales_data()` | 配置 + 日期 | 当天销售 DataFrame |
| ② | `anomaly_detector.py` | `detect_anomalies()` | 当天数据 | 异常产品清单 + 对比日期 |
| ③ | `analyzer.py` | `analyze_anomalies()` | 异常清单 | AI 分析结论列表 |
| ④ | `reporter.py` | `generate_report()` | 全部数据 | Markdown 报告文件路径 |
| ⑤ | `notifier.py` | `send_notification()` | 报告路径 | 钉钉 / 邮箱推送 |

**数据在各步骤间的流向：**

```
config.json ──▶ ① 读 Excel / MySQL → DataFrame
                         │
                         ▼
                 ② 环比计算 → 标记异常 → (异常清单, 对比日期)
                         │
                         ▼
                 ③ 构建提示词 → 调用 DeepSeek → AI 分析结论
                         │
                         ▼
                 ④ 统计 + 排版 → Markdown 报告文件
                         │
                         ▼
                 ⑤ 生成摘要 → 钉钉 Webhook / 邮件 SMTP
```

#### `src/data_fetcher.py` — 数据拉取

| 函数 | 可见性 | 作用 |
|------|--------|------|
| `fetch_sales_data(config, target_date)` | **公开** | 入口，根据配置自动选 Excel 或 MySQL |
| `fetch_from_excel(config, target_date)` | 私有 | 扫描 data/ 目录，读取匹配日期的 .xlsx |
| `fetch_from_mysql(config, target_date)` | 私有 | 直连 MySQL 查询 |
| `_normalize_columns(df)` | 私有 | 英文列名 → 中文列名 |
| `_validate_columns(df)` | 私有 | 检查必要列是否齐全（日进货量可选） |

**支持的数据列**：日期、产品名称、销售量、销售额、单价、库存、日进货量（可选）

#### `src/anomaly_detector.py` — 异常检测

| 函数 | 可见性 | 作用 |
|------|--------|------|
| `detect_anomalies(config, sales_data)` | **公开** | 入口，执行全部检测规则 |
| `_try_load_previous_day(config, df)` | 私有 | 加载上期数据做环比（支持智能回退） |

**4 条检测规则：**
1. **环比检测**：销量变化 ≥ ±30% → 异常上涨 / 下跌
2. **零销量检测**：上期有销量本期归零 → 断崖式下跌
3. **库存不足检测**：进货量不足 + 库存紧张 → 预警
4. **同比检测**（预留）：对比去年同期

**智能回退机制**：前一日无数据时，自动扫描历史文件取最近一期。

#### `src/analyzer.py` — AI 归因分析

| 函数 | 可见性 | 作用 |
|------|--------|------|
| `analyze_anomalies(config, anomalies, sales_data, comparison_date)` | **公开** | 调用 DeepSeek API 做归因分析 |
| `_build_analysis_prompt(anomalies, sales_data, comparison_date)` | 私有 | 构建发送给 AI 的分析提示词 |

**分析维度**：库存因素、进货因素、价格因素、趋势判断、关联分析、建议措施

#### `src/reporter.py` — 报告生成

| 函数 | 可见性 | 作用 |
|------|--------|------|
| `generate_report(config, sales_data, anomalies, analysis_results, target_date, comparison_date)` | **公开** | 生成 Markdown 报告 |
| `threshold(config)` | 工具 | 读取异常阈值 |

**报告结构**：① 总体概况 → ② 异常商品明细表 → ③ AI 归因分析 → ④ 重点关注

#### `src/notifier.py` — 推送通知

| 函数 | 可见性 | 作用 |
|------|--------|------|
| `send_notification(config, report_path, anomaly_count, target_date)` | **公开** | 推送入口 |
| `_generate_summary(...)` | 私有 | 从报告提取摘要 |
| `_send_dingtalk(...)` | 私有 | 钉钉 Webhook 推送 |
| `_send_email(...)` | 私有 | 邮件 SMTP 推送 |

### scripts/ 辅助工具

| 文件 | 作用 | 什么时候用 |
|------|------|------------|
| `init_db.sql` | 创建 sales 数据库 + daily_sales 表 + 14 条测试数据 | 第一次部署时在 MySQL 中执行 |
| `export_mysql_to_excel.py` | 从 MySQL 导出指定日期的数据为 Excel | 手动导出或 main.py 自动调用 |

---

# 第二部分：dashboard/ — Streamlit 可视化管理面板

## 架构分层（五层单向依赖）

```
视图层 (app.py, pages/*.py)
    │
    ▼
业务服务层 (services/agent_service.py, data_service.py)
    │
    ├──────────────┐
    ▼              ▼
数据访问层        后端模块
(dao/db.py)      (backend/src/*.py)
    │
    ▼
MySQL 数据库
```

**单向依赖规则**：上层可以调用下层，下层不能调用上层，禁止跨层调用。

## 目录结构

```
dashboard/
├── app.py                        ← 🏠 主入口 + 首页（数据概览看板）
├── .env.example                  ← 🔐 数据库连接配置模板
├── requirements_dashboard.txt    ← 📦 面板额外依赖
│
├── pages/                        ← 📄 页面（Streamlit 自动识别）
│   ├── 2_🤖_任务控制.py           ← 智能体任务参数 + 启动
│   ├── 3_📋_数据管理.py           ← 数据增删改查 + CSV 导出
│   └── 4_📋_运行日志.py           ← 执行历史日志查看
│
├── services/                     ← 🔗 业务服务层
│   ├── __init__.py
│   ├── agent_service.py          ← 智能体调用 + 后台线程
│   └── data_service.py           ← 数据查询/统计/增删改查
│
├── dao/                          ← 🗄️ 数据访问层
│   ├── __init__.py
│   └── db.py                     ← 单例数据库连接 + SQL 操作
│
└── utils/                        ← 🧰 通用工具层
    ├── __init__.py
    ├── config.py                 ← .env 环境变量读取
    ├── ui_components.py          ← 可复用 UI 组件
    └── common.py                 ← 日期格式化、CSV 导出等工具
```

## 文件逐一说明

### 入口

| 文件 | 层级 | 作用 |
|------|------|------|
| `app.py` | 视图层 | 面板入口，设置全局路径，渲染侧边栏和首页 |
| `.env.example` | 配置 | 数据库连接信息模板，复制为 `.env` 填入真实值 |

### pages/ 页面

| 文件 | 对应功能 |
|------|----------|
| `app.py`（首页） | 数据概览看板：4 个指标卡片 + 趋势折线图 + 分类饼图 |
| `2_🤖_任务控制.py` | 参数表单 → 二次确认 → 后台启动智能体 → 实时状态 |
| `3_📋_数据管理.py` | 筛选查询（分页表格）+ 新增表单 + 删除 + CSV 导出 |
| `4_📋_运行日志.py` | 本地记录 / 数据库日志查看，支持状态和日期筛选 |

### services/ 业务服务层

| 文件 | 核心函数 | 作用 |
|------|----------|------|
| `agent_service.py` | `start_agent_task()` `get_agent_status()` | 后台线程调用 backend/ 智能体 |
| `data_service.py` | `get_dashboard_stats()` `query_data()` `insert_data()` | 数据库统计与增删改查 |

### dao/ 数据访问层

| 文件 | 核心类 | 作用 |
|------|--------|------|
| `db.py` | `Database`（单例） | `get_connection()` / `execute_query()` / `execute_write()` |

### utils/ 通用工具层

| 文件 | 作用 |
|------|------|
| `config.py` | 从 `.env` 读取数据库连接配置 |
| `ui_components.py` | `metric_card()` / `confirm_dialog()` / `status_badge()` / `data_table()` |
| `common.py` | `fmt_date()` / `fmt_number()` / `fmt_duration()` / `df_to_csv_download()` |

---

# 第三部分：学习路线建议

## 入门（了解系统做什么）

1. 先读 `CLAUDE.md` —— 了解项目整体概况
2. 打开 `backend/config.json` —— 看有哪些可配置项
3. 运行 `python backend/main.py` —— 看一次完整执行过程

## 核心（理解数据分析引擎）

4. 读 `backend/src/data_fetcher.py` —— 数据从哪里来
5. 读 `backend/src/anomaly_detector.py` —— 怎么判断异常
6. 读 `backend/src/analyzer.py` —— AI 怎么分析
7. 读 `backend/main.py` —— 5 步如何串联

## 进阶（理解可视化面板）

8. 读 `dashboard/app.py` —— 面板入口和首页
9. 读 `dashboard/services/data_service.py` —— 数据怎么到页面
10. 读 `dashboard/services/agent_service.py` —— 后台任务怎么管理

## 改代码时的安全原则

| 层级 | 可以改什么 | 不能改什么 |
|------|-----------|-----------|
| 视图层 `pages/` | UI 布局、图表样式、表单字段 | SQL 语句、业务逻辑 |
| 服务层 `services/` | 参数校验、业务规则、结果包装 | 数据库连接代码 |
| 数据层 `dao/` | 数据库操作封装 | 业务规则 |
| 后端 `src/` | 分析算法、检测规则、报告格式 | - |
